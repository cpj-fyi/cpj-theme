{{!< default}}

{{!--
    Single post template
    Detects if post is essay or radar and adjusts layout accordingly
--}}

{{#post}}
<article class="post {{post_class}}">
    <header class="post-header">
        {{#if primary_tag}}
            <a href="{{primary_tag.url}}" class="post-tag">{{primary_tag.name}}</a>
        {{/if}}

        <h1 class="post-title">{{title}}</h1>

        {{#if custom_excerpt}}
            <p class="post-excerpt">{{custom_excerpt}}</p>
        {{/if}}

        <div class="post-meta ds-meta">
            {{#foreach authors}}
                <span class="ds-meta-item">{{name}}</span>
            {{/foreach}}
            <span class="ds-meta-item">{{date format="MMMM D, YYYY"}}</span>
            {{#if reading_time}}
                <span class="ds-meta-item">{{reading_time}}</span>
            {{/if}}
        </div>
    </header>

    {{#if feature_image}}
        <figure class="post-feature-image">
            <img src="{{feature_image}}" alt="{{title}}">
            {{#if feature_image_caption}}
                <figcaption>{{feature_image_caption}}</figcaption>
            {{/if}}
        </figure>
    {{/if}}

    <div class="post-body">
        <div class="content-wrapper{{#unless @member}}{{#has visibility="paid,members"}} content-gated{{/has}}{{/unless}}">
            <div class="content">
                {{content}}
            </div>

            {{!-- Membership CTA for non-members on paid content --}}
            {{#unless @member}}
                {{#has visibility="paid"}}
                    {{> "membership-cta"}}
                {{/has}}
                {{#has visibility="members"}}
                    {{> "membership-cta"}}
                {{/has}}
            {{/unless}}
        </div>

        {{!-- Tags --}}
        {{#if tags}}
            <div class="post-tags ds-meta">
                {{#foreach tags}}
                    <a href="{{url}}" class="ds-meta-item">{{name}}</a>
                {{/foreach}}
            </div>
        {{/if}}
    </div>

    {{!-- Related posts --}}
    {{#get "posts" filter="id:-{{id}}+tag:{{primary_tag.slug}}" limit="3" include="tags,authors"}}
        {{#if posts}}
            <section class="related-posts">
                <div class="container">
                    <div class="section-header">
                        <h2 class="section-title">Related</h2>
                    </div>
                    <div class="related-posts-grid">
                        {{#foreach posts}}
                            <article class="post-card post-card-text-only">
                                <div class="ds-meta-tag-wrapper">
                                    {{#foreach tags}}
                                        {{#match slug "essays"}}
                                        {{else match slug "radar"}}
                                        {{else match slug "five-things"}}
                                        {{else}}
                                            <span class="ds-meta-item">{{name}}</span>
                                        {{/match}}
                                    {{/foreach}}
                                </div>
                                <h3 class="post-card-title">
                                    <a href="{{url}}">{{title}}</a>
                                </h3>
                                <p class="post-card-excerpt">{{excerpt}}</p>
                                <div class="ds-meta">
                                    <span class="ds-meta-item">{{date format="MMM D, YYYY"}}</span>
                                    {{#if reading_time}}
                                        <span class="ds-meta-item">{{reading_time}}</span>
                                    {{/if}}
                                </div>
                            </article>
                        {{/foreach}}
                    </div>
                </div>
            </section>
        {{/if}}
    {{/get}}
</article>
{{/post}}
