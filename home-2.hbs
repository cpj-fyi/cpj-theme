{{!--
    Alternative homepage at /new/
    See docs/superpowers/specs/2026-05-21-homepage-redesign-design.md
--}}
{{!< default}}

<link rel="stylesheet" type="text/css" href="{{asset "css/home-2.css"}}">

<main class="h2-page">

  {{!-- ============ Pub masthead (centered) ============ --}}
  <header class="h2-pubmast">
    <div class="h2-container">
      <h1 class="h2-pubmast-title">Clay Parker Jones</h1>
      {{#if @site.description}}
        <p class="h2-pubmast-subhead">{{@site.description}}</p>
      {{/if}}
    </div>
  </header>

  {{!-- ============ Top main: 3-column (Radar rail · Lead · Secondary) ============ --}}
  <section class="h2-section h2-topmain-section">
    <div class="h2-container h2-topmain">

      {{!-- Left rail: Radar --}}
      <aside class="h2-rail h2-rail-radar">
        <header class="h2-rail-head">
          <span class="radar-dot"></span>
          <h2 class="h2-flag h2-rail-flag">Radar</h2>
        </header>
        {{#get "posts" filter="tag:radar" limit="5" include="tags"}}
          {{#if posts}}
            <ol class="h2-rail-list">
              {{#foreach posts}}
                <li class="h2-rail-item">
                  <time class="h2-rail-date" datetime="{{date format="YYYY-MM-DD"}}">{{date format="DD MMM"}}</time>
                  <h3 class="h2-rail-title"><a href="{{url}}">{{title}}</a></h3>
                </li>
              {{/foreach}}
            </ol>
            <a class="h2-rail-more" href="/radar/">More on Radar →</a>
          {{/if}}
        {{/get}}
      </aside>

      {{!-- Center: Lead story (latest essay) --}}
      <article class="h2-lead">
        {{#get "posts" filter="tag:essays" limit="1" include="tags"}}
          {{#if posts}}
            {{#foreach posts}}
              <p class="h2-flag h2-lead-flag">Lead Essay</p>
              <h2 class="h2-lead-title">
                <a href="{{url}}">{{title}}</a>
              </h2>
              {{#if custom_excerpt}}
                <p class="h2-lead-deck">{{custom_excerpt}}</p>
              {{else}}
                <p class="h2-lead-deck">{{excerpt words="40"}}</p>
              {{/if}}
              <p class="h2-lead-byline">
                <span>By {{primary_author.name}}</span>
                <span aria-hidden="true">·</span>
                <span>{{reading_time}}</span>
                <span aria-hidden="true">·</span>
                <span>{{date format="DD MMM YY"}}</span>
              </p>
              <figure class="h2-lead-hero">
                <img src="{{#if feature_image}}{{feature_image}}{{else}}{{@custom.art_url}}/{{slug}}.svg{{/if}}"
                     alt=""
                     loading="eager"
                     width="1200"
                     height="630">
              </figure>
              <div class="h2-lead-lede">
                {{content words="120"}}
              </div>
              <p class="h2-lead-continue">
                <a href="{{url}}">Continue reading →</a>
              </p>
            {{/foreach}}
          {{else}}
            <p class="h2-lead-empty">No essays yet — back soon.</p>
          {{/if}}
        {{/get}}
      </article>

      {{!-- Right: Story 2 (second-most-recent essay) --}}
      <article class="h2-story2">
        {{#get "posts" filter="tag:essays" limit="2" include="tags"}}
          {{#if posts}}
            {{#foreach posts from="2"}}
              <p class="h2-flag h2-story2-flag">Also Reading</p>
              <h3 class="h2-story2-title">
                <a href="{{url}}">{{title}}</a>
              </h3>
              {{#if custom_excerpt}}
                <p class="h2-story2-deck">{{custom_excerpt}}</p>
              {{else}}
                <p class="h2-story2-deck">{{excerpt words="35"}}</p>
              {{/if}}
              <p class="h2-story2-byline">
                <span>{{reading_time}}</span>
                <span aria-hidden="true">·</span>
                <span>{{date format="DD MMM YY"}}</span>
              </p>
              {{#if feature_image}}
                <figure class="h2-story2-hero">
                  <img src="{{feature_image}}" alt="" loading="lazy" width="800" height="500">
                </figure>
              {{/if}}
              <p class="h2-story2-continue">
                <a href="{{url}}">Continue reading →</a>
              </p>
            {{/foreach}}
          {{/if}}
        {{/get}}
      </article>

    </div>
  </section>

  {{!-- ============ AI stripe ============ --}}
  <section class="h2-section h2-stripe h2-stripe-ai">
    <hr class="h2-rule">
    <div class="h2-container">
      <header class="h2-stripe-head">
        <div class="h2-stripe-head-text">
          <h2 class="h2-stripe-flag">AI</h2>
          <p class="h2-stripe-tagline">How generative AI is reshaping the work — and the org.</p>
        </div>
        <a class="h2-stripe-seeall" href="/tag/ai/">See all →</a>
      </header>
      {{#get "posts" filter="tag:ai" limit="3" include="tags"}}
        {{#if posts}}
          <div class="h2-stripe-grid">
            {{#foreach posts}}
              <article class="h2-stripe-card">
                {{#if feature_image}}
                  <a class="h2-stripe-card-image-link" href="{{url}}" aria-hidden="true" tabindex="-1">
                    <figure class="h2-stripe-card-image">
                      <img src="{{feature_image}}" alt="" loading="lazy" width="800" height="500">
                    </figure>
                  </a>
                {{/if}}
                <h3 class="h2-stripe-card-title"><a href="{{url}}">{{title}}</a></h3>
                <p class="h2-stripe-card-meta">
                  <span>{{date format="DD MMM YY"}}</span>
                  <span aria-hidden="true">·</span>
                  <span>{{reading_time}}</span>
                </p>
              </article>
            {{/foreach}}
          </div>
        {{else}}
          <p class="h2-stripe-empty">No AI posts yet.</p>
        {{/if}}
      {{/get}}
    </div>
  </section>

  {{!-- ============ Org Design stripe ============ --}}
  <section class="h2-section h2-stripe h2-stripe-orgdesign">
    <hr class="h2-rule">
    <div class="h2-container">
      <header class="h2-stripe-head">
        <div class="h2-stripe-head-text">
          <h2 class="h2-stripe-flag">Org Design</h2>
          <p class="h2-stripe-tagline">How organizations actually work — and how to design them better.</p>
        </div>
        <a class="h2-stripe-seeall" href="/tag/org-design/">See all →</a>
      </header>
      {{#get "posts" filter="tag:org-design" limit="3" include="tags"}}
        {{#if posts}}
          <div class="h2-stripe-grid">
            {{#foreach posts}}
              <article class="h2-stripe-card">
                {{#if feature_image}}
                  <a class="h2-stripe-card-image-link" href="{{url}}" aria-hidden="true" tabindex="-1">
                    <figure class="h2-stripe-card-image">
                      <img src="{{feature_image}}" alt="" loading="lazy" width="800" height="500">
                    </figure>
                  </a>
                {{/if}}
                <h3 class="h2-stripe-card-title"><a href="{{url}}">{{title}}</a></h3>
                <p class="h2-stripe-card-meta">
                  <span>{{date format="DD MMM YY"}}</span>
                  <span aria-hidden="true">·</span>
                  <span>{{reading_time}}</span>
                </p>
              </article>
            {{/foreach}}
          </div>
        {{else}}
          <p class="h2-stripe-empty">No Org Design posts yet.</p>
        {{/if}}
      {{/get}}
    </div>
  </section>

  {{!-- ============ Subscribe bar ============ --}}
  <section class="h2-sub" data-mode="portal">
    <hr class="h2-rule">
    <div class="h2-container h2-sub-inner">
      <div class="h2-sub-pitch">
        <h2 class="h2-sub-heading">Subscribe.</h2>
        <p class="h2-sub-tagline">Pick what arrives.</p>
      </div>
      <div class="h2-sub-form">
        <a href="#/portal/signup" class="h2-sub-row" data-portal="signup" aria-label="Open signup">
          <span class="h2-sub-input-mock" aria-hidden="true">your@email.com</span>
          <span class="h2-sub-submit" aria-hidden="true">Subscribe →</span>
        </a>
        <ul class="h2-sub-options" aria-hidden="true">
          <li class="h2-sub-option is-checked">
            <span class="h2-sub-check"></span>
            <span class="h2-sub-label"><strong>Essays</strong> — Long-form pieces, every few weeks.</span>
          </li>
          <li class="h2-sub-option is-checked">
            <span class="h2-sub-check"></span>
            <span class="h2-sub-label"><strong>Five Things</strong> — Short list-format posts.</span>
          </li>
          <li class="h2-sub-option is-checked">
            <span class="h2-sub-check"></span>
            <span class="h2-sub-label"><strong>Radar</strong> — Daily-ish observations on org design.</span>
          </li>
        </ul>
      </div>
    </div>
    <hr class="h2-rule">
  </section>

  {{!-- ============ Hidden Patterns module ============ --}}
  <section class="h2-section h2-book-section">
    <div class="h2-container h2-book">
      <div class="h2-book-cover-wrap">
        <img class="h2-book-cover"
             src="{{asset "images/hidden-patterns-cover.png"}}"
             alt="Hidden Patterns book cover"
             loading="lazy">
      </div>
      <div class="h2-book-copy">
        <p class="h2-flag h2-book-flag">The Book</p>
        <h2 class="h2-book-title">Hidden Patterns</h2>
        <p class="h2-book-deck">
          Most &ldquo;people problems&rdquo; are org design problems. This book shows you how to fix them for good.
        </p>
        <blockquote class="h2-book-pullquote">
          <p>&ldquo;If <em>A Pattern Language</em> was for buildings, <em>Hidden Patterns</em> is for the invisible architecture of work.&rdquo;</p>
          <cite class="h2-book-pullquote-cite">&mdash; Mick McConnell, Head of Space, Airbnb</cite>
        </blockquote>
        <div class="h2-book-actions">
          <a class="h2-btn h2-btn-primary" href="/buy/">Buy the book &rarr;</a>
          <a class="h2-btn h2-btn-text" href="/foundations/">Read a chapter</a>
        </div>
        <p class="h2-book-inside">
          <span class="h2-book-inside-label">Inside:</span>
          <a href="/start-end/">Start &amp; End</a> &middot;
          <a href="/foundations/">Foundations</a> &middot;
          <a href="/structuring/">Structuring</a> &middot;
          <a href="/direction/">Direction</a> &middot;
          <a href="/practice/">Practice</a> &middot;
          <a href="/learning/">Learning</a> &middot;
          <a href="/space/">Space</a>
        </p>
      </div>
    </div>
  </section>

</main>
