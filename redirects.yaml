# CPJ Theme Redirects
# Upload this file via Ghost Admin > Settings > Labs > Redirects
#
# Redirects old post URLs from /{slug}/ to /posts/{slug}/
# List specific posts here since pages also use root-level URLs

301:
  # Add specific old post redirects here, e.g.:
  # ^/the-undercurrent-skills-maturity-matrix/?$: /posts/the-undercurrent-skills-maturity-matrix/
  # ^/another-old-post/?$: /posts/another-old-post/
