{{!< default}}

{{!--
    Radar archive template
    Compact feed layout for shorter posts
--}}

<header class="archive-header">
    <div class="container">
        <h1 class="archive-title">Radar</h1>
        {{#if tag.description}}
            <p class="archive-description">{{tag.description}}</p>
        {{else}}
            <p class="archive-description">Quick takes, links, and observations.</p>
        {{/if}}
        <div class="archive-count">{{plural pagination.total empty="No posts" singular="% post" plural="% posts"}}</div>
    </div>
</header>

<div class="container">
    <div class="radar-layout">
        {{#if posts}}
            <div class="radar-list">
                {{#foreach posts}}
                    {{> "post-card-radar"}}
                {{/foreach}}
            </div>
        {{else}}
            <p>No radar posts yet.</p>
        {{/if}}

        {{pagination}}
    </div>
</div>
